name: Update content.json from Google Apps Script

on:
  workflow_dispatch:
  schedule:
    - cron: "*/10 * * * *"  # cada 10 min

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Fetch JSON from Apps Script
        run: |
          mkdir -p assets/data
          curl -L --fail "https://script.google.com/macros/s/AKfycbwQRrNRJ4fF4kpiyUG4w8HYVp68JbSW2SmpTImz3PCwsP_15nvMoMk-f2sRVdgKYVP1/exec" \
            -o assets/data/content.json

      - name: Commit changes (if any)
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add assets/data/content.json
          git diff --cached --quiet || git commit -m "chore: update content.json"
          git push
